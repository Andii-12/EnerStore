# React App Environment Variables
# Copy this file to .env.local for local development
# For Vercel, set these in the Vercel dashboard

# API Base URL (Optional - will auto-detect if not set)
# For local development: REACT_APP_API_URL=http://localhost:5000
# For Railway only: REACT_APP_API_URL=https://enerstore-production.up.railway.app
# Leave empty for auto-detection: REACT_APP_API_URL=

# Socket.io Server URL (Optional - will auto-detect if not set)
# For local development: REACT_APP_SOCKET_URL=http://localhost:5000
# For Railway only: REACT_APP_SOCKET_URL=https://enerstore-production.up.railway.app
# Leave empty for auto-detection: REACT_APP_SOCKET_URL=

# Environment
REACT_APP_ENV=development

# Auto-detection behavior:
# 1. If REACT_APP_API_URL is set, use that
# 2. If not set, try localhost:5000 first
# 3. If localhost:5000 fails, fallback to Railway
# 4. This gives you the best of both worlds!

# Optional: Analytics (if you add them later)
# REACT_APP_GA_TRACKING_ID=G-XXXXXXXXXX 